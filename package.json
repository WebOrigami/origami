{
  "name": "@weborigami/monorepo",
  "version": "0.3.4-jse.7",
  "workspaces": [
    "async-tree",
    "language",
    "origami",
    "types"
  ],
  "scripts": {
    "build": "cd language; npm run build",
    "publish-all": "cd types; npm publish --tag test; cd ../async-tree; npm publish --tag test; cd ../language; npm publish --tag test; cd ../origami; npm publish --tag test",
    "test": "node --test --test-reporter=spec",
    "typecheck": "tsc"
  }
}
