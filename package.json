{
  "name": "@weborigami/monorepo",
  "version": "0.5.6",
  "workspaces": [
    "async-tree",
    "language",
    "origami"
  ],
  "scripts": {
    "build": "cd language; npm run build",
    "publish-all": "cd ../async-tree; npm publish --tag=test; cd ../language; npm publish --tag=test; cd ../origami; npm publish --tag=test",
    "test": "node --test --test-reporter=spec",
    "typecheck": "tsc"
  }
}
