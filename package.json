{
  "name": "@weborigami/monorepo",
  "version": "0.6.11",
  "workspaces": [
    "async-tree",
    "language",
    "origami"
  ],
  "scripts": {
    "build": "cd language; npm run build",
    "publish-all": "cd async-tree; npm publish; cd ../language; npm publish; cd ../origami; npm publish",
    "test": "node --test --test-reporter=spec",
    "typecheck": "tsc"
  }
}
