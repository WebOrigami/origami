{
  "name": "@graphorigami/monorepo",
  "version": "0.0.30",
  "workspaces": [
    "core",
    "origami",
    "types"
  ],
  "scripts": {
    "prepublishOnly": "pushd origami; npm run prepublishOnly; popd",
    "test": "node --test --test-reporter=spec */test/*.test.js */test/*/*.test.js */test/*/*/*.test.js",
    "typecheck": "tsc"
  }
}
